// three-shim.d.ts
// This shim is intentionally left empty. The previous content forced CommonJS-style `export =` for the
// "three" module, which breaks named imports. With modern Three.js (>= r160) and its typings,
// no shim is needed.
export {};
